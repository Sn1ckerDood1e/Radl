---
phase: 33-legal-pages
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - src/components/layout/site-footer.tsx
  - src/app/(dashboard)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "User sees footer with Terms and Privacy links on dashboard pages"
    - "User can click Terms link to navigate to /terms"
    - "User can click Privacy link to navigate to /privacy"
  artifacts:
    - path: "src/components/layout/site-footer.tsx"
      provides: "Reusable footer component"
      min_lines: 20
      exports: ["SiteFooter"]
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Dashboard layout with footer"
      contains: "SiteFooter"
  key_links:
    - from: "src/components/layout/site-footer.tsx"
      to: "/terms"
      via: "Link component"
      pattern: "href.*terms"
    - from: "src/components/layout/site-footer.tsx"
      to: "/privacy"
      via: "Link component"
      pattern: "href.*privacy"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/site-footer.tsx"
      via: "import and render"
      pattern: "import.*SiteFooter"
---

<objective>
Create footer component with legal links and integrate into dashboard layout.

Purpose: Ensure users can always access legal pages from within the app (LEGL-03 requirement).
Output: Reusable SiteFooter component rendered on all authenticated pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-legal-pages/33-01-SUMMARY.md

# Existing patterns
@src/components/layout/dashboard-header.tsx (styling reference)
@src/app/(dashboard)/layout.tsx (where footer will be added)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SiteFooter component</name>
  <files>src/components/layout/site-footer.tsx</files>
  <action>
Create src/components/layout/site-footer.tsx as a simple, reusable footer.

Component requirements:
- Client component ('use client') - not needed, can be server component
- Actually make it a server component (no 'use client') since it has no interactivity
- Named export: SiteFooter

Structure:
```
<footer className="border-t border-[var(--border-subtle)] bg-[var(--surface-1)]">
  <div className="container mx-auto px-4 py-4">
    <div className="flex flex-col sm:flex-row items-center justify-between gap-2 text-sm text-[var(--text-muted)]">
      {/* Copyright */}
      <span>© 2026 Radl, Inc.</span>

      {/* Legal Links */}
      <nav className="flex items-center gap-4">
        <Link href="/terms" className="hover:text-teal-600 transition-colors">
          Terms of Service
        </Link>
        <Link href="/privacy" className="hover:text-teal-600 transition-colors">
          Privacy Policy
        </Link>
      </nav>
    </div>
  </div>
</footer>
```

Styling notes:
- Match dashboard header styling (same border color, bg color, container)
- Use teal-600 for hover state on links (brand color)
- Subtle, unobtrusive - users need to find it but it shouldn't compete with content
- Mobile: stack vertically (flex-col), Desktop: horizontal (sm:flex-row)
- py-4 for comfortable touch targets on mobile
  </action>
  <verify>
Run TypeScript check: `npx tsc --noEmit`
Verify component exports correctly: grep for "export function SiteFooter" in file
  </verify>
  <done>
SiteFooter component exists at src/components/layout/site-footer.tsx.
Component exports SiteFooter function.
Links to /terms and /privacy are present.
Styling matches brand (teal-600 hover, consistent with header).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add footer to dashboard layout</name>
  <files>src/app/(dashboard)/layout.tsx</files>
  <action>
Modify src/app/(dashboard)/layout.tsx to include the SiteFooter.

Changes:
1. Add import at top: `import { SiteFooter } from '@/components/layout/site-footer';`

2. In the return JSX, add SiteFooter after </main> and before the Toaster:

Current structure:
```jsx
<div className="min-h-screen bg-[var(--background)] transition-colors">
  <DashboardHeader team={legacyTeam} contexts={contexts} />
  <main className="flex-1">
    {children}
  </main>
  <Toaster ... />
</div>
```

New structure:
```jsx
<div className="min-h-screen bg-[var(--background)] transition-colors flex flex-col">
  <DashboardHeader team={legacyTeam} contexts={contexts} />
  <main className="flex-1">
    {children}
  </main>
  <SiteFooter />
  <Toaster ... />
</div>
```

Important: Add `flex flex-col` to the wrapper div so footer stays at bottom when content is short.

Note: The Toaster should remain last (after footer) so toast notifications appear above everything.
Note: Do NOT add footer to mobile bottom navigation area - footer is separate from nav.
  </action>
  <verify>
Run `npm run build` and verify no errors.
Navigate to any dashboard page and scroll to bottom - footer should be visible.
Click "Terms of Service" link - should navigate to /terms.
Click "Privacy Policy" link - should navigate to /privacy.
  </verify>
  <done>
Dashboard layout imports and renders SiteFooter.
Footer appears at bottom of all dashboard pages.
Links navigate to /terms and /privacy correctly.
Footer does not interfere with mobile bottom navigation.
  </done>
</task>

</tasks>

<verification>
After completing both tasks:

1. Build check: `npm run build` completes without errors
2. Footer visible: Navigate to any dashboard page, scroll to bottom, footer is present
3. Terms link: Click "Terms of Service" in footer, navigates to /terms
4. Privacy link: Click "Privacy Policy" in footer, navigates to /privacy
5. Mobile check: Footer appears above bottom navigation (if present), links are tappable
</verification>

<success_criteria>
- LEGL-03: Footer links to Terms and Privacy on all authenticated pages
- Footer is visually consistent with brand (teal-600 accents)
- Footer does not break existing layout or mobile navigation
</success_criteria>

<output>
After completion, create `.planning/phases/33-legal-pages/33-02-SUMMARY.md`
</output>
