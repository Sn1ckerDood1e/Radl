---
phase: 16-ui-ux-polish
plan: 06
type: execute
wave: 3
depends_on: ["16-01", "16-02", "16-03", "16-04", "16-05"]
files_modified:
  - src/app/(dashboard)/[teamSlug]/regattas/page.tsx
  - src/app/(dashboard)/[teamSlug]/schedule/loading.tsx
  - src/app/(dashboard)/[teamSlug]/regattas/loading.tsx
autonomous: false

must_haves:
  truths:
    - "All requirements UIX-02 through UIX-09 are satisfied"
    - "All list views have consistent empty states"
    - "All main pages have skeleton loading"
    - "Forms validate on blur with inline feedback"
    - "Command palette accessible globally"
    - "Onboarding flow works for new users"
  artifacts:
    - path: "src/app/(dashboard)/[teamSlug]/regattas/page.tsx"
      provides: "Regattas page with EmptyState"
      contains: "EmptyState"
    - path: "src/app/(dashboard)/[teamSlug]/schedule/loading.tsx"
      provides: "Schedule page skeleton"
      exports: ["default"]
    - path: "src/app/(dashboard)/[teamSlug]/regattas/loading.tsx"
      provides: "Regattas page skeleton"
      exports: ["default"]
  key_links: []
---

<objective>
Complete remaining polish items and verify all UI/UX requirements.

Purpose: Ensure complete coverage of all UIX requirements by adding remaining loading skeletons and empty states, then verify the full feature set.

Output: Complete UI/UX polish with all 7 requirements verified working.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-ui-ux-polish/16-CONTEXT.md
@.planning/phases/16-ui-ux-polish/16-RESEARCH.md

Reference prior plan outputs:
@.planning/phases/16-ui-ux-polish/16-01-SUMMARY.md
@.planning/phases/16-ui-ux-polish/16-02-SUMMARY.md
@.planning/phases/16-ui-ux-polish/16-03-SUMMARY.md
@.planning/phases/16-ui-ux-polish/16-04-SUMMARY.md
@.planning/phases/16-ui-ux-polish/16-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add remaining loading skeletons and empty states</name>
  <files>
    src/app/(dashboard)/[teamSlug]/schedule/loading.tsx
    src/app/(dashboard)/[teamSlug]/regattas/loading.tsx
    src/app/(dashboard)/[teamSlug]/regattas/page.tsx
  </files>
  <action>
1. Create `src/app/(dashboard)/[teamSlug]/schedule/loading.tsx`:
   - Calendar-style skeleton with month header
   - Grid of day cells with skeleton event indicators
   - Match the schedule page layout

2. Create `src/app/(dashboard)/[teamSlug]/regattas/loading.tsx`:
   - Header with title and "New Regatta" button skeleton
   - List of regatta cards with name, date, location skeletons

3. Update `src/app/(dashboard)/[teamSlug]/regattas/page.tsx`:
   - Import EmptyState and Trophy icon
   - Replace inline empty state with EmptyState component:
     ```tsx
     <EmptyState
       icon={Trophy}
       title="No regattas yet"
       description="Create your first regatta to start planning race day."
       action={isCoach ? { label: "New Regatta", href: `/${teamSlug}/regattas/new` } : undefined}
     />
     ```
  </action>
  <verify>
    - `ls src/app/(dashboard)/[teamSlug]/schedule/loading.tsx` exists
    - `ls src/app/(dashboard)/[teamSlug]/regattas/loading.tsx` exists
    - `grep -n "EmptyState" src/app/(dashboard)/[teamSlug]/regattas/page.tsx` shows usage
    - `pnpm tsc --noEmit` passes
  </verify>
  <done>
    Schedule and regattas pages have loading skeletons. Regattas page uses EmptyState component.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete UI/UX polish implementation across 6 plans:
    - Empty states for all list views (UIX-02)
    - Skeleton loading for main pages (UIX-03)
    - Error handling with retry actions (UIX-04)
    - Form validation with inline feedback (UIX-05)
    - Micro-animations for state transitions (UIX-07)
    - Onboarding flow for new users (UIX-08)
    - Command palette and keyboard shortcuts (UIX-09)
  </what-built>
  <how-to-verify>
Test each requirement:

1. **Empty States (UIX-02):**
   - Visit roster page with no members -> see empty state with icon and message
   - Visit equipment page with no items -> see empty state with "Add Equipment" button
   - Visit practices page with no practices -> see empty state
   - Visit practice-templates, lineup-templates, regattas with empty data

2. **Skeleton Loading (UIX-03):**
   - Hard refresh (Cmd+Shift+R) on roster page -> see skeleton placeholders
   - Hard refresh equipment page -> see skeleton
   - Hard refresh practices page -> see skeleton
   - Verify skeletons match content layout (no visual jump when data loads)

3. **Error Handling (UIX-04):**
   - Open DevTools Network tab, set to Offline
   - Try to save something (invite member, create team) -> see error toast with Retry button
   - Click Retry -> toast shows retry attempt
   - Error toasts should persist until dismissed

4. **Form Validation (UIX-05):**
   - Go to create team form
   - Click in name field, then click out without typing -> see inline error
   - Start typing -> error clears
   - Tab through all required fields -> each shows error on blur

5. **Micro-animations (UIX-07):**
   - Error messages animate in (fade + slide from top)
   - Success checkmarks zoom in
   - Dialog open/close has smooth animation
   - Toast notifications slide in

6. **Onboarding (UIX-08):**
   - Clear localStorage: `localStorage.removeItem('radl-onboarding-{teamId}')`
   - Refresh team dashboard as coach -> onboarding wizard appears
   - See "Step 1 of 4" with progress dots
   - Click through steps, verify navigation works
   - Click X to skip -> wizard closes
   - Refresh page -> onboarding does NOT show again

7. **Keyboard Shortcuts (UIX-09):**
   - Press Cmd+K (Mac) or Ctrl+K (Windows) -> command palette opens
   - Type to filter commands
   - Use arrow keys to navigate, Enter to select
   - Press Escape to close
   - Press ? key -> shortcuts overlay appears
   - Press G then R -> navigates to roster
   - Press G then P -> navigates to practices
  </how-to-verify>
  <resume-signal>Type "verified" if all requirements work, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
All 7 requirements verified manually:
1. UIX-02: Empty states with icons, messages, action buttons
2. UIX-03: Skeleton placeholders during loading
3. UIX-04: Error toasts with Retry action, persist until dismissed
4. UIX-05: Form validation on blur, inline errors, red border styling
5. UIX-07: Micro-animations on errors, success, dialogs
6. UIX-08: Onboarding wizard with steps, progress, skip
7. UIX-09: Cmd+K command palette, ? shortcuts, G+key navigation
</verification>

<success_criteria>
- All 7 UIX requirements verified working
- No TypeScript errors
- No console errors during testing
- Consistent visual experience across all tested flows
- Phase ready for completion marking
</success_criteria>

<output>
After completion, create `.planning/phases/16-ui-ux-polish/16-06-SUMMARY.md`
</output>
