---
phase: 14-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/select.tsx
  - src/components/ui/input.tsx
  - src/components/ui/dropdown-menu.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "Button component renders with correct theme colors"
    - "Dialog component opens and closes correctly"
    - "Select component shows options and handles selection"
    - "Input component accepts text and shows focus state"
    - "DropdownMenu component shows menu items on trigger"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "Button component with variants"
      exports: ["Button", "buttonVariants"]
    - path: "src/components/ui/dialog.tsx"
      provides: "Dialog component with accessibility"
      exports: ["Dialog", "DialogContent", "DialogHeader", "DialogTitle", "DialogDescription", "DialogFooter", "DialogTrigger", "DialogClose"]
    - path: "src/components/ui/select.tsx"
      provides: "Select component with Radix"
      exports: ["Select", "SelectContent", "SelectItem", "SelectTrigger", "SelectValue"]
    - path: "src/components/ui/input.tsx"
      provides: "Input component"
      exports: ["Input"]
    - path: "src/components/ui/dropdown-menu.tsx"
      provides: "DropdownMenu component"
      exports: ["DropdownMenu", "DropdownMenuContent", "DropdownMenuItem", "DropdownMenuTrigger"]
  key_links:
    - from: "src/components/ui/button.tsx"
      to: "src/lib/utils.ts"
      via: "cn utility import"
      pattern: "import.*cn.*from.*@/lib/utils"
    - from: "src/components/ui/dialog.tsx"
      to: "@radix-ui/react-dialog"
      via: "Radix Dialog primitive"
      pattern: "@radix-ui/react-dialog"
---

<objective>
Install the top 5 high-traffic shadcn/ui components: Button, Dialog, Select, Input, DropdownMenu.

Purpose: Provide consistent, accessible UI primitives for the application.
Output: Five component files in src/components/ui/, Radix dependencies installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase-specific context
@.planning/phases/14-design-system-foundation/14-CONTEXT.md
@.planning/phases/14-design-system-foundation/14-RESEARCH.md

# Prior plan summary
@.planning/phases/14-design-system-foundation/14-01-SUMMARY.md

# Configuration file created in Plan 01
@components.json
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn/ui components</name>
  <files>src/components/ui/button.tsx, src/components/ui/dialog.tsx, src/components/ui/select.tsx, src/components/ui/input.tsx, src/components/ui/dropdown-menu.tsx, package.json</files>
  <action>
Install all five components in a single command (CLI resolves dependencies automatically):

```bash
npx shadcn@latest add button dialog select input dropdown-menu
```

This will:
1. Create src/components/ui/button.tsx
2. Create src/components/ui/dialog.tsx
3. Create src/components/ui/select.tsx
4. Create src/components/ui/input.tsx
5. Create src/components/ui/dropdown-menu.tsx
6. Install Radix UI dependencies (@radix-ui/react-dialog, @radix-ui/react-select, @radix-ui/react-dropdown-menu, @radix-ui/react-slot, etc.)

IMPORTANT:
- If prompted about overwriting export-button.tsx, say NO - that's an existing unrelated component
- The CLI installs to src/components/ui/ by default per components.json
  </action>
  <verify>
- All five component files exist in src/components/ui/
- Each file has 'use client' directive at top
- package.json includes @radix-ui/react-dialog, @radix-ui/react-select, @radix-ui/react-dropdown-menu
- npm install completes successfully
  </verify>
  <done>Five shadcn/ui components installed with all Radix dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Verify component rendering</name>
  <files></files>
  <action>
Run build to verify all components compile:
```bash
npm run build
```

Then start dev server and create a quick test page or use browser DevTools console to verify imports work:
```bash
npm run dev
```

In browser DevTools console (on any page), verify imports resolve:
- Check no TypeScript errors in terminal
- Check no React errors in browser console

If there are import errors:
- Verify components.json has correct aliases
- Verify tsconfig.json has @/* path alias (should already exist)
  </action>
  <verify>
- npm run build completes without TypeScript errors
- No import resolution errors in terminal
- Dev server starts and pages render
  </verify>
  <done>All shadcn/ui components compile and can be imported</done>
</task>

</tasks>

<verification>
- [ ] src/components/ui/button.tsx exists with Button export
- [ ] src/components/ui/dialog.tsx exists with Dialog, DialogContent, etc. exports
- [ ] src/components/ui/select.tsx exists with Select, SelectContent, etc. exports
- [ ] src/components/ui/input.tsx exists with Input export
- [ ] src/components/ui/dropdown-menu.tsx exists with DropdownMenu exports
- [ ] package.json includes @radix-ui/react-* dependencies
- [ ] npm run build passes
- [ ] Each component has 'use client' directive
</verification>

<success_criteria>
Five shadcn/ui components (Button, Dialog, Select, Input, DropdownMenu) are installed and compile successfully. All Radix UI dependencies are present. Build passes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-system-foundation/14-02-SUMMARY.md`
</output>
