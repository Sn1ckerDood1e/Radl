---
phase: 34-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/[teamSlug]/roster/[id]/loading.tsx
  - src/app/(dashboard)/[teamSlug]/equipment/[id]/loading.tsx
  - src/app/(dashboard)/[teamSlug]/practices/[id]/loading.tsx
autonomous: true

must_haves:
  truths:
    - "User navigating to roster/[id] sees skeleton placeholders during load"
    - "User navigating to equipment/[id] sees skeleton placeholders during load"
    - "User navigating to practices/[id] sees skeleton placeholders during load"
  artifacts:
    - path: "src/app/(dashboard)/[teamSlug]/roster/[id]/loading.tsx"
      provides: "Roster detail skeleton loading"
      exports: ["default"]
    - path: "src/app/(dashboard)/[teamSlug]/equipment/[id]/loading.tsx"
      provides: "Equipment detail skeleton loading"
      exports: ["default"]
    - path: "src/app/(dashboard)/[teamSlug]/practices/[id]/loading.tsx"
      provides: "Practice detail skeleton loading"
      exports: ["default"]
  key_links:
    - from: "loading.tsx files"
      to: "src/components/ui/skeleton.tsx"
      via: "import { Skeleton }"
      pattern: "import.*Skeleton.*from"
---

<objective>
Create loading.tsx skeleton states for detail pages (roster/[id], equipment/[id], practices/[id]) to provide visual feedback during navigation.

Purpose: Implements LOAD-02 requirement. Users see skeleton placeholders instead of blank screens when navigating to detail pages.

Output:
- loading.tsx files in each detail page directory
- Skeleton layouts that match the actual page structure
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-ux-polish/34-RESEARCH.md

# Existing loading patterns
@src/app/(dashboard)/[teamSlug]/equipment/loading.tsx
@src/app/(dashboard)/[teamSlug]/roster/loading.tsx

# Detail page structure to match
@src/app/(dashboard)/[teamSlug]/roster/[id]/page.tsx
@src/app/(dashboard)/[teamSlug]/equipment/[id]/page.tsx
@src/app/(dashboard)/[teamSlug]/practices/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create roster detail loading.tsx</name>
  <files>src/app/(dashboard)/[teamSlug]/roster/[id]/loading.tsx</files>
  <action>
Create skeleton loading state for the athlete profile detail page.

Examine profile-client.tsx to understand the page structure, then create matching skeleton:

Expected structure (based on typical profile page):
1. Breadcrumb area (roster > athlete name)
2. Avatar + name + role badge header
3. Profile details card with labeled fields
4. Optional sections (contact info, notes, etc.)

Implementation:
```tsx
import { Skeleton } from "@/components/ui/skeleton"

export default function RosterDetailLoading() {
  return (
    <div className="max-w-4xl mx-auto">
      {/* Breadcrumb */}
      <div className="flex items-center gap-2 mb-6">
        <Skeleton className="h-4 w-16" />
        <span className="text-zinc-600">/</span>
        <Skeleton className="h-4 w-32" />
      </div>

      {/* Header with avatar */}
      <div className="flex items-center gap-4 mb-8">
        <Skeleton className="h-20 w-20 rounded-full" />
        <div>
          <Skeleton className="h-8 w-48 mb-2" />
          <Skeleton className="h-6 w-24 rounded-full" />
        </div>
      </div>

      {/* Profile card */}
      <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-6 space-y-6">
        {/* Contact section */}
        <div>
          <Skeleton className="h-5 w-32 mb-4" />
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
            {Array.from({ length: 4 }).map((_, i) => (
              <div key={i}>
                <Skeleton className="h-4 w-20 mb-2" />
                <Skeleton className="h-5 w-40" />
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
  )
}
```

Adjust skeleton dimensions after reading profile-client.tsx to match actual layout.
  </action>
  <verify>
1. File exists at src/app/(dashboard)/[teamSlug]/roster/[id]/loading.tsx
2. File imports Skeleton component
3. File exports default function
  </verify>
  <done>
loading.tsx exists in roster/[id]/, renders skeleton matching profile detail structure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create equipment detail loading.tsx</name>
  <files>src/app/(dashboard)/[teamSlug]/equipment/[id]/loading.tsx</files>
  <action>
Create skeleton loading state for the equipment detail page.

Examine equipment/[id]/page.tsx to understand the page structure, then create matching skeleton.

Expected structure (based on equipment detail pages):
1. Breadcrumb area (equipment > item name)
2. Name + status badge header
3. Equipment details card (type, manufacturer, weight class, etc.)
4. Damage history section
5. QR code section

Implementation pattern:
```tsx
import { Skeleton } from "@/components/ui/skeleton"

export default function EquipmentDetailLoading() {
  return (
    <div className="max-w-4xl mx-auto">
      {/* Breadcrumb */}
      <div className="flex items-center gap-2 mb-6">
        <Skeleton className="h-4 w-20" />
        <span className="text-zinc-600">/</span>
        <Skeleton className="h-4 w-32" />
      </div>

      {/* Header with badge */}
      <div className="flex items-center justify-between mb-6">
        <div className="flex items-center gap-3">
          <Skeleton className="h-8 w-48" />
          <Skeleton className="h-6 w-20 rounded-full" />
        </div>
        <Skeleton className="h-9 w-24" /> {/* Edit button */}
      </div>

      {/* Details card */}
      <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-6 mb-6">
        <Skeleton className="h-6 w-32 mb-4" />
        <div className="grid grid-cols-2 md:grid-cols-3 gap-4">
          {Array.from({ length: 6 }).map((_, i) => (
            <div key={i}>
              <Skeleton className="h-4 w-20 mb-2" />
              <Skeleton className="h-5 w-28" />
            </div>
          ))}
        </div>
      </div>

      {/* Damage history section */}
      <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-6">
        <Skeleton className="h-6 w-40 mb-4" />
        <div className="space-y-3">
          {Array.from({ length: 3 }).map((_, i) => (
            <div key={i} className="flex items-center gap-3">
              <Skeleton className="h-8 w-8 rounded" />
              <div className="flex-1">
                <Skeleton className="h-4 w-48 mb-1" />
                <Skeleton className="h-3 w-24" />
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
```

Adjust after reading page.tsx to match actual layout.
  </action>
  <verify>
1. File exists at src/app/(dashboard)/[teamSlug]/equipment/[id]/loading.tsx
2. File imports Skeleton component
3. File exports default function
  </verify>
  <done>
loading.tsx exists in equipment/[id]/, renders skeleton matching equipment detail structure.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create practices detail loading.tsx</name>
  <files>src/app/(dashboard)/[teamSlug]/practices/[id]/loading.tsx</files>
  <action>
Create skeleton loading state for the practice detail page.

Examine practices/[id]/page.tsx and practice-detail-client.tsx to understand the page structure. Practice pages typically have:

1. Breadcrumb area (practices > date)
2. Practice header (date, time, status)
3. Block list (multiple blocks with lineups)
4. Action buttons

Implementation pattern:
```tsx
import { Skeleton } from "@/components/ui/skeleton"

export default function PracticeDetailLoading() {
  return (
    <div className="max-w-4xl mx-auto">
      {/* Breadcrumb */}
      <div className="flex items-center gap-2 mb-6">
        <Skeleton className="h-4 w-20" />
        <span className="text-zinc-600">/</span>
        <Skeleton className="h-4 w-32" />
      </div>

      {/* Header */}
      <div className="flex items-center justify-between mb-6">
        <div>
          <Skeleton className="h-8 w-48 mb-2" />
          <Skeleton className="h-5 w-32" />
        </div>
        <div className="flex gap-2">
          <Skeleton className="h-9 w-24" />
          <Skeleton className="h-9 w-24" />
        </div>
      </div>

      {/* Practice info card */}
      <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-6 mb-6">
        <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
          {Array.from({ length: 4 }).map((_, i) => (
            <div key={i}>
              <Skeleton className="h-4 w-16 mb-2" />
              <Skeleton className="h-5 w-24" />
            </div>
          ))}
        </div>
      </div>

      {/* Blocks section */}
      <div className="space-y-4">
        <Skeleton className="h-6 w-24 mb-2" />
        {Array.from({ length: 2 }).map((_, i) => (
          <div key={i} className="bg-zinc-900 border border-zinc-800 rounded-lg p-4">
            <div className="flex items-center justify-between mb-4">
              <Skeleton className="h-6 w-32" />
              <Skeleton className="h-5 w-20" />
            </div>
            <div className="grid grid-cols-2 gap-2">
              {Array.from({ length: 4 }).map((_, j) => (
                <Skeleton key={j} className="h-10 w-full rounded" />
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  )
}
```

Adjust after reading page.tsx and practice-detail-client.tsx to match actual layout.
  </action>
  <verify>
1. File exists at src/app/(dashboard)/[teamSlug]/practices/[id]/loading.tsx
2. File imports Skeleton component
3. File exports default function
  </verify>
  <done>
loading.tsx exists in practices/[id]/, renders skeleton matching practice detail structure.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run TypeScript check: `npx tsc --noEmit`
2. Verify all loading.tsx files exist:
   - ls src/app/(dashboard)/[teamSlug]/roster/[id]/loading.tsx
   - ls src/app/(dashboard)/[teamSlug]/equipment/[id]/loading.tsx
   - ls src/app/(dashboard)/[teamSlug]/practices/[id]/loading.tsx
3. Run dev server and navigate to detail pages - should see skeletons during load
</verification>

<success_criteria>
1. All three detail page directories have loading.tsx files
2. Skeleton layouts match the actual page structures
3. TypeScript compiles without errors
4. User navigating to detail pages sees skeleton placeholders during load
</success_criteria>

<output>
After completion, create `.planning/phases/34-ux-polish/34-02-SUMMARY.md`
</output>
