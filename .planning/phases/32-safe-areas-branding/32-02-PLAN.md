---
phase: 32-safe-areas-branding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/**/*.tsx
  - src/app/**/*.tsx
autonomous: true

must_haves:
  truths:
    - "User sees brand teal (#0d9488) as the primary accent color"
    - "No emerald color classes remain in UI components"
    - "Active states, buttons, and badges use teal-500/600 consistently"
  artifacts:
    - path: "src/components/layout/bottom-navigation.tsx"
      provides: "Navigation with teal active state"
      contains: "teal-500"
    - path: "src/components/layout/dashboard-header.tsx"
      provides: "Header with teal fallback badge"
      contains: "teal-600"
  key_links:
    - from: "src/components/**/*.tsx"
      to: "Tailwind teal palette"
      via: "class names"
      pattern: "teal-(400|500|600)"
---

<objective>
Migrate color palette from emerald to teal across the entire codebase.

Purpose: Align all UI colors with Radl brand teal (#0d9488) for visual consistency.

Output: All 474 emerald class occurrences replaced with equivalent teal classes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-safe-areas-branding/32-RESEARCH.md
@.planning/BRANDING.md

Key findings from research:
- 474 occurrences of emerald-* classes across 165 files (including .planning docs)
- Source files only: ~280 occurrences across ~100 files
- Main patterns: emerald-500 (main accent), emerald-600 (darker), emerald-400 (lighter)
- Opacity variants: emerald-500/20, emerald-500/10 for backgrounds
- CSS variables already use teal: --brand-primary: #0d9488, --team-primary: #0d9488
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace emerald with teal in source files</name>
  <files>src/**/*.tsx</files>
  <action>
Use sed to perform batch replacement of emerald -> teal in all source files:

```bash
# From /home/hb/radl directory
find src -name "*.tsx" -type f -exec sed -i 's/emerald-/teal-/g' {} +
```

This handles all patterns:
- emerald-400 -> teal-400
- emerald-500 -> teal-500
- emerald-600 -> teal-600
- emerald-500/20 -> teal-500/20 (opacity variants preserved)
- bg-emerald-*, text-emerald-*, border-emerald-*, etc.

After replacement, verify no emerald references remain:
```bash
grep -r "emerald-" src/ --include="*.tsx" | wc -l
# Should return 0
```

Do NOT modify files in .planning/ directory - those are historical documentation.
  </action>
  <verify>
Zero emerald occurrences in src: `grep -r "emerald-" src/ --include="*.tsx" | wc -l` returns 0
TypeScript compiles: `npx tsc --noEmit` passes (Tailwind classes are strings, no type errors expected)
  </verify>
  <done>
All emerald-* classes in src/ replaced with teal-* equivalents
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify critical component colors</name>
  <files>
src/components/layout/bottom-navigation.tsx
src/components/layout/dashboard-header.tsx
src/components/layout/navigation-sidebar.tsx
  </files>
  <action>
After batch replacement, manually verify these high-visibility components have correct teal classes:

1. bottom-navigation.tsx - Active nav item should be text-teal-500 (was emerald-500)
2. dashboard-header.tsx - Team initial fallback badge should be bg-teal-600 (was emerald-600)
3. navigation-sidebar.tsx - Active sidebar item should use teal accent

Read each file and confirm teal classes are present. If any were missed or incorrectly replaced, fix manually.

Expected patterns after migration:
- Active states: text-teal-500, bg-teal-500
- Darker accents: teal-600
- Lighter accents: teal-400
- Backgrounds with opacity: teal-500/20, teal-500/10
  </action>
  <verify>
Grep critical files:
- `grep "teal-" src/components/layout/bottom-navigation.tsx` shows teal-500
- `grep "teal-" src/components/layout/dashboard-header.tsx` shows teal-600
- `grep "teal-" src/components/layout/navigation-sidebar.tsx` shows teal classes
  </verify>
  <done>
All three navigation components use teal classes for active/accent states
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verification</name>
  <files>None (verification only)</files>
  <action>
Run full build to ensure no issues from color migration:

```bash
cd /home/hb/radl && npm run build
```

Common issues to watch for:
- None expected (Tailwind classes are just strings)
- If build fails, check for typos introduced during sed replacement

Also run a final count to confirm migration scope:
```bash
# Count teal occurrences to verify migration happened
grep -r "teal-" src/ --include="*.tsx" | wc -l
# Should be approximately same as original emerald count (~280)
```
  </action>
  <verify>
Build succeeds: `npm run build` completes without errors
Teal count: `grep -r "teal-" src/ --include="*.tsx" | wc -l` returns ~280 (similar to original emerald count)
  </verify>
  <done>
Build passes, teal classes present throughout codebase at expected count
  </done>
</task>

</tasks>

<verification>
1. Zero emerald-* classes in src/ directory
2. Build succeeds without errors
3. Teal class count matches expected (~280 occurrences)
4. Critical navigation components confirmed to have teal active states
</verification>

<success_criteria>
- BRND-02: Color palette updated to brand teal (#0d9488) as primary color
- All UI elements use teal-* classes instead of emerald-*
- Visual consistency with brand palette defined in BRANDING.md
</success_criteria>

<output>
After completion, create `.planning/phases/32-safe-areas-branding/32-02-SUMMARY.md`
</output>
