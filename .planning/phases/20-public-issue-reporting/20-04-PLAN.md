---
phase: 20-public-issue-reporting
plan: 04
type: execute
wave: 3
depends_on: [20-02, 20-03]
files_modified:
  - src/components/equipment/qr-code-display.tsx
  - src/components/equipment/equipment-detail.tsx
  - src/app/(dashboard)/[teamSlug]/equipment/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Equipment detail page shows QR code for damage reporting"
    - "User can download QR code as PNG"
    - "QR code has equipment name label for printing"
    - "Download button available to coaches only"
  artifacts:
    - path: "src/components/equipment/qr-code-display.tsx"
      provides: "QR code display with download functionality"
      exports: ["QRCodeDisplay"]
    - path: "src/components/equipment/equipment-detail.tsx"
      provides: "Equipment detail component with QR code section"
  key_links:
    - from: "src/components/equipment/equipment-detail.tsx"
      to: "src/components/equipment/qr-code-display.tsx"
      via: "component import and render"
      pattern: "QRCodeDisplay"
---

<objective>
Add individual QR code generation with download capability to the equipment detail page.

Purpose: Enable coaches to generate and download QR codes for individual equipment items to print as stickers for physical placement on boats and oars.
Output: Enhanced QRCodeDisplay component with download button, integrated into equipment detail page for coach access.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-public-issue-reporting/20-CONTEXT.md
@.planning/phases/20-public-issue-reporting/20-RESEARCH.md
@.planning/phases/20-public-issue-reporting/20-02-SUMMARY.md
@src/components/equipment/qr-code-display.tsx
@src/components/equipment/equipment-detail.tsx
@src/app/(dashboard)/[teamSlug]/equipment/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance QRCodeDisplay with download functionality</name>
  <files>src/components/equipment/qr-code-display.tsx</files>
  <action>
Extend the QRCodeDisplay component with downloadable QR code:

```typescript
'use client';

import { useRef, useCallback } from 'react';
import { QRCodeSVG } from 'qrcode.react';

interface QRCodeDisplayProps {
  equipmentId: string;
  equipmentName?: string;
  size?: number;
  showDownload?: boolean;
}

export function QRCodeDisplay({
  equipmentId,
  equipmentName,
  size = 128,
  showDownload = false,
}: QRCodeDisplayProps) {
  const qrRef = useRef<SVGSVGElement>(null);

  // Use environment variable for base URL, fallback to window.location.origin for dev
  const baseUrl = process.env.NEXT_PUBLIC_APP_URL || (typeof window !== 'undefined' ? window.location.origin : '');
  const reportUrl = `${baseUrl}/report/${equipmentId}`;

  const handleDownload = useCallback(() => {
    if (!qrRef.current) return;

    // Create a larger canvas for print quality (300 DPI equivalent)
    const scaleFactor = 3;
    const padding = 20 * scaleFactor;
    const labelHeight = equipmentName ? 40 * scaleFactor : 0;
    const canvasSize = size * scaleFactor;
    const totalWidth = canvasSize + padding * 2;
    const totalHeight = canvasSize + padding * 2 + labelHeight;

    const canvas = document.createElement('canvas');
    canvas.width = totalWidth;
    canvas.height = totalHeight;
    const ctx = canvas.getContext('2d');
    if (!ctx) return;

    // White background
    ctx.fillStyle = '#ffffff';
    ctx.fillRect(0, 0, totalWidth, totalHeight);

    // Convert SVG to image and draw on canvas
    const svgData = new XMLSerializer().serializeToString(qrRef.current);
    const svgBlob = new Blob([svgData], { type: 'image/svg+xml;charset=utf-8' });
    const svgUrl = URL.createObjectURL(svgBlob);

    const img = new Image();
    img.onload = () => {
      // Draw QR code centered
      ctx.drawImage(img, padding, padding, canvasSize, canvasSize);

      // Add equipment name label if provided
      if (equipmentName) {
        ctx.fillStyle = '#1f2937';
        ctx.font = `bold ${14 * scaleFactor}px -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif`;
        ctx.textAlign = 'center';
        ctx.fillText(
          equipmentName,
          totalWidth / 2,
          canvasSize + padding + 28 * scaleFactor,
          totalWidth - padding
        );
      }

      // Trigger download
      const link = document.createElement('a');
      const fileName = equipmentName
        ? `qr-${equipmentName.toLowerCase().replace(/\s+/g, '-')}.png`
        : `qr-${equipmentId.substring(0, 8)}.png`;
      link.download = fileName;
      link.href = canvas.toDataURL('image/png');
      link.click();

      URL.revokeObjectURL(svgUrl);
    };
    img.src = svgUrl;
  }, [equipmentId, equipmentName, size]);

  return (
    <div className="inline-block">
      <div className="p-4 bg-white rounded-lg border border-gray-200">
        <QRCodeSVG
          ref={qrRef}
          value={reportUrl}
          size={size}
          level="M"
        />
        {equipmentName && (
          <p className="mt-2 text-sm font-medium text-gray-900 text-center truncate max-w-[128px]">
            {equipmentName}
          </p>
        )}
        <p className="mt-1 text-xs text-gray-500 text-center">
          Scan to report damage
        </p>
      </div>

      {showDownload && (
        <button
          type="button"
          onClick={handleDownload}
          className="mt-2 w-full flex items-center justify-center gap-2 px-3 py-2 text-sm font-medium text-gray-700 bg-white border border-gray-300 rounded-md hover:bg-gray-50"
        >
          <svg className="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
          </svg>
          Download PNG
        </button>
      )}
    </div>
  );
}
```

Key features:
- ref on QRCodeSVG to access SVG element
- Canvas-based PNG export at 3x resolution for print quality
- Equipment name rendered as label below QR code
- Clean filename based on equipment name
- Download button conditionally shown via showDownload prop
  </action>
  <verify>QRCodeDisplay renders with download button; clicking downloads PNG with equipment name label</verify>
  <done>QRCodeDisplay has download functionality producing print-quality PNG with equipment label</done>
</task>

<task type="auto">
  <name>Task 2: Add QR code section to equipment detail component</name>
  <files>src/components/equipment/equipment-detail.tsx</files>
  <action>
Add a QR Code section to the equipment detail component, visible to coaches only:

1. Import QRCodeDisplay:
```typescript
import { QRCodeDisplay } from './qr-code-display';
```

2. Add QR Code section after the existing equipment details (before any action buttons):
```tsx
{/* QR Code for Damage Reporting - Coaches only */}
{isCoach && (
  <div className="bg-surface-2 rounded-lg p-6">
    <h3 className="text-lg font-medium text-white mb-4">QR Code</h3>
    <div className="flex flex-col sm:flex-row items-start sm:items-center gap-4">
      <QRCodeDisplay
        equipmentId={equipment.id}
        equipmentName={equipment.name}
        size={120}
        showDownload
      />
      <div className="text-sm text-zinc-400">
        <p className="mb-2">
          Print this QR code and attach it to the equipment.
        </p>
        <p>
          Anyone can scan it to report damage directly — no login required.
        </p>
      </div>
    </div>
  </div>
)}
```

The QR code section:
- Uses same bg-surface-2 styling as other sections
- Shows equipment name on the QR code
- Includes download button
- Has helpful text explaining the purpose
- Only visible to coaches (isCoach prop)
  </action>
  <verify>Equipment detail page shows QR code section for coaches; QR code has equipment name and download button</verify>
  <done>Equipment detail component includes QR code section with download capability for coaches</done>
</task>

<task type="auto">
  <name>Task 3: Pass equipment name to detail component</name>
  <files>src/app/(dashboard)/[teamSlug]/equipment/[id]/page.tsx</files>
  <action>
Verify the equipment name is being passed to the EquipmentDetail component.

Looking at the existing code, equipmentData already includes the name field:
```typescript
const equipmentData = {
  id: equipment.id,
  type: equipment.type,
  name: equipment.name,  // Already included
  // ...
};
```

And it's passed to EquipmentDetail:
```tsx
<EquipmentDetail
  equipment={equipmentData}
  teamSlug={teamSlug}
  isCoach={isCoach}
/>
```

No changes needed to the page component - the equipment name is already available.

However, verify that the EquipmentDetail component's type includes all needed fields. If the component needs updating to use equipment.name for the QR code, that's handled in Task 2.
  </action>
  <verify>Equipment name flows through to QRCodeDisplay; QR code shows equipment name</verify>
  <done>Equipment name available in detail component for QR code generation</done>
</task>

</tasks>

<verification>
1. Navigate to `/[teamSlug]/equipment/[id]` as coach - QR code section visible
2. Navigate as athlete - QR code section NOT visible
3. QR code shows equipment name below it
4. Click "Download PNG" - file downloads with equipment name in filename
5. Open downloaded PNG - shows QR code with label, high resolution for printing
6. Scan downloaded QR code - navigates to public report page
</verification>

<success_criteria>
- QRCodeDisplay accepts equipmentName and showDownload props
- Download produces PNG at 3x resolution (print quality)
- PNG includes equipment name label below QR code
- Equipment detail page shows QR section only to coaches
- Section includes helpful instructions text
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/20-public-issue-reporting/20-04-SUMMARY.md`
</output>
