---
phase: 17-facility-ui-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/facility/[facilitySlug]/page.tsx
  - src/app/(dashboard)/facility/[facilitySlug]/loading.tsx
autonomous: true

must_haves:
  truths:
    - "Facility dashboard shows aggregate statistics (clubs, athletes, equipment, upcoming events)"
    - "Dashboard has card grid navigation to Clubs, Equipment, Events, Settings"
    - "Stats cards display real counts from database"
    - "Loading state shows skeleton placeholders"
  artifacts:
    - path: "src/app/(dashboard)/facility/[facilitySlug]/page.tsx"
      provides: "Enhanced facility dashboard with card grid navigation"
      min_lines: 150
    - path: "src/app/(dashboard)/facility/[facilitySlug]/loading.tsx"
      provides: "Skeleton loading state for facility dashboard"
      min_lines: 20
  key_links:
    - from: "src/app/(dashboard)/facility/[facilitySlug]/page.tsx"
      to: "prisma"
      via: "database queries for stats"
      pattern: "prisma\\.(team|equipment|practice)"
---

<objective>
Enhance the facility dashboard with card grid navigation and aggregate statistics.

Purpose: Facility admins need a central hub to navigate facility management features. The existing dashboard shows basic stats but lacks navigation cards and upcoming events count (FAC-09).

Output: Enhanced facility dashboard matching club dashboard pattern with large clickable cards for Clubs, Shared Equipment, Events, and Settings.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-facility-ui-features/17-CONTEXT.md
@src/app/(dashboard)/[teamSlug]/page.tsx
@src/app/(dashboard)/facility/[facilitySlug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance facility dashboard with card grid navigation</name>
  <files>src/app/(dashboard)/facility/[facilitySlug]/page.tsx</files>
  <action>
Replace the existing facility dashboard page with an enhanced version following the club dashboard pattern.

Key changes:
1. Keep existing auth checks (viewMode === 'facility', FACILITY_ADMIN role)
2. Add upcoming events query (practices in next 7 days across all clubs)
3. Replace the "Quick Actions" section with large clickable navigation cards

Stats row should show:
- Total Clubs (count)
- Total Athletes (sum of members across all clubs)
- Shared Equipment (facility-owned + club isShared)
- Upcoming Events (practices in next 7 days)

Navigation card grid (2x2 on mobile, 4 cols on desktop):
1. **Clubs** - href to `/facility/{facilitySlug}/clubs` - icon: Building2 - count: clubs.length
2. **Shared Equipment** - href to `/facility/{facilitySlug}/equipment` - icon: Ship - count: total shared
3. **Events** - href to `/facility/{facilitySlug}/events` - icon: Calendar - count: upcoming
4. **Settings** - href to `/facility/{facilitySlug}/settings` - icon: Settings - no count

Card styling should match club dashboard:
- bg-[var(--surface-1)] hover:bg-[var(--surface-2)]
- rounded-xl p-6
- border border-[var(--border-subtle)] hover:border-[var(--border)]
- transition-all duration-200

Keep the existing clubs list section but move it below the navigation cards.

Use lucide-react icons: Building2, Ship, Calendar, Settings
  </action>
  <verify>Run `npm run build` - page should compile. Visit /facility/{slug} and verify card grid displays</verify>
  <done>Facility dashboard shows stats row with 4 metrics, navigation card grid with 4 cards linking to sub-pages, and clubs list below</done>
</task>

<task type="auto">
  <name>Task 2: Add loading skeleton for facility dashboard</name>
  <files>src/app/(dashboard)/facility/[facilitySlug]/loading.tsx</files>
  <action>
Create a loading.tsx file with skeleton placeholders matching the dashboard layout:

```tsx
import { Skeleton } from '@/components/ui/skeleton';

export default function FacilityDashboardLoading() {
  return (
    <div className="max-w-6xl mx-auto">
      {/* Header skeleton */}
      <div className="mb-8">
        <div className="flex items-center gap-3 mb-2">
          <Skeleton className="h-12 w-12 rounded-lg" />
          <div>
            <Skeleton className="h-8 w-48 mb-2" />
            <Skeleton className="h-4 w-32" />
          </div>
        </div>
      </div>

      {/* Stats row skeleton */}
      <div className="grid grid-cols-2 md:grid-cols-4 gap-4 mb-8">
        {[1, 2, 3, 4].map((i) => (
          <div key={i} className="bg-[var(--surface-1)] rounded-xl p-4 border border-[var(--border-subtle)]">
            <Skeleton className="h-8 w-16 mb-2" />
            <Skeleton className="h-4 w-24" />
          </div>
        ))}
      </div>

      {/* Navigation cards skeleton */}
      <div className="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-8">
        {[1, 2, 3, 4].map((i) => (
          <div key={i} className="bg-[var(--surface-1)] rounded-xl p-6 border border-[var(--border-subtle)]">
            <Skeleton className="h-14 w-14 rounded-xl mb-4" />
            <Skeleton className="h-6 w-24 mb-2" />
            <Skeleton className="h-4 w-32" />
          </div>
        ))}
      </div>

      {/* Clubs section skeleton */}
      <div className="mb-8">
        <Skeleton className="h-6 w-20 mb-4" />
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
          {[1, 2, 3].map((i) => (
            <div key={i} className="bg-[var(--surface-1)] rounded-xl p-5 border border-[var(--border-subtle)]">
              <div className="flex items-center gap-3 mb-3">
                <Skeleton className="h-10 w-10 rounded-lg" />
                <div>
                  <Skeleton className="h-5 w-32 mb-1" />
                  <Skeleton className="h-3 w-20" />
                </div>
              </div>
              <Skeleton className="h-4 w-40" />
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
```

This skeleton mirrors the actual layout to prevent visual shift on load.
  </action>
  <verify>Navigate to /facility/{slug} with slow connection simulation - skeleton should display before content loads</verify>
  <done>Loading skeleton exists and matches dashboard layout structure</done>
</task>

</tasks>

<verification>
1. Build: `npm run build` passes
2. Dashboard loads: Navigate to /facility/{facilitySlug} - shows stats and navigation cards
3. Stats accurate: Club count, athlete count, equipment count match database
4. Navigation works: Each card links to correct sub-page (pages may 404 until created)
5. Loading state: Skeleton displays during initial load
</verification>

<success_criteria>
- Stats row shows 4 metrics: Clubs, Total Athletes, Shared Equipment, Upcoming Events
- Navigation card grid has 4 cards: Clubs, Shared Equipment, Events, Settings
- Cards follow club dashboard styling pattern
- Loading skeleton matches layout structure
- All links use correct facility slug in URL
</success_criteria>

<output>
After completion, create `.planning/phases/17-facility-ui-features/17-02-SUMMARY.md`
</output>
